# raw match data (170MB, restorable from cricsheet.org) — anchored to the repo
# root on purpose: web/static/data/ (the pipeline's emitted contract files)
# MUST be committed for GitHub Pages deploys.
/data/

# python
__pycache__/
*.pyc
.venv/

# node / sveltekit
node_modules/
web/.svelte-kit/
web/build/

# os / editor
.DS_Store
*.log
